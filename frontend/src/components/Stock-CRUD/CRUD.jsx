import React from 'react'

const CRUD = () => {
    return (
        // const response = await fetch '../data.json'
        // const data = await response.json()
        
        <div>            

            <h1>Hola desde componente CRUD</h1>
        </div>
    )
}

export default CRUD
